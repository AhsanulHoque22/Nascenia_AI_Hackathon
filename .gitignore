venv/
__pycache__/
*.pyc
.ipynb_checkpoints/

# Large data - not committed, re-downloaded via Kaggle
data/raw/
data/processed/*
!data/processed/.gitkeep
data/external/*
!data/external/.gitkeep

# Experiment artifacts / checkpoints - large, not committed
experiments/*/
!experiments/.gitkeep

# Analysis outputs that embed raw competition data (rulebook S2 forbids
# redistribution; S10 notes winners may be asked to open-source their code)
experiments/test_train_overlap.csv

# Kaggle submissions
submissions/*.csv

# Model weights (Phase 2 package should document a download path, not commit weights)
phase2_package/weights/
*.bin
*.safetensors
*.pt
*.ckpt

.env
.DS_Store

# Mirror of data/processed/ used only to upload as a private Kaggle dataset
# (regenerate with: cp data/processed/*.jsonl kaggle_dataset_processed/)
kaggle_dataset_processed/
